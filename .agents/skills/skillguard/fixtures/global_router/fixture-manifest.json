{
  "schema_version": "skillguard.fixture_manifest.v1",
  "fixture_version": "global-router-v1",
  "checker_version": "skillguard.local_cli_dispatch.v1",
  "current_status": {
    "status": "current",
    "notes": [
      "Exercises the author-side global SkillGuard registry refresh and currentness path against explicit local roots and a fixture-local Codex home."
    ]
  },
  "fixtures": [
    {
      "fixture_id": "global-router-refresh-pass",
      "fixture_type": "positive",
      "target_rule": "Refreshing the global router writes a registry, prompt projection, managed AGENTS block, and passes prompt freshness checks.",
      "expected_decision": "pass",
      "status": "checked",
      "path": "cases/refresh-global-router-pass.json"
    },
    {
      "fixture_id": "global-router-check-registry-stale-roots-fail",
      "fixture_type": "negative",
      "target_rule": "A registry checked against a different current root reports stale registry state.",
      "expected_decision": "fail",
      "status": "checked",
      "path": "cases/check-global-registry-stale-roots-fail.json"
    }
  ],
  "evidence": [
    "cases/refresh-global-router-pass.json",
    "cases/check-global-registry-stale-roots-fail.json",
    "workspace/global_router/global_registry.json",
    "workspace/codex_home/AGENTS.md"
  ],
  "skipped_checks": [
    "These fixtures cover the author-side composite refresh and stale registry root detection only; retired ordinary task resolution and standalone prompt commands are intentionally absent.",
    "They do not prove broad fixture coverage, user machine installation state, package publication, release readiness, external services, or future AI behavior."
  ],
  "residual_risk": [
    "Future author-side refresh or prompt-template changes may require refreshing fixture workspace evidence.",
    "The fixture-local Codex home is not proof of the real user's installed prompt state."
  ],
  "claim_boundary": "This global-router fixture manifest covers only the listed local command cases and fixture workspace files. It does not prove runtime checker execution beyond this fixture invocation, broad fixture coverage, CLI checks outside the explicit command surface, tests, suite automation, package publication, code-contract validation, release readiness, external service behavior, real user prompt state, or future AI behavior without separate current evidence."
}
