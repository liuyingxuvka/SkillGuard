## 1. Freeze Authority And Update Existing Models

- [x] 1.1 Record the clean SkillGuard source revision, dirty peer worktrees, installed-skill inventory, and `.skillguard` content classifications without modifying peer work.
- [x] 1.2 Update the declared-check supervision model with maintenance-unit, member, evidence-subject, and cross-unit receipt rejection scenarios.
- [x] 1.3 Contract the validation-composition model so owner reuse is same-unit only and OpenSpec/cross-unit receipt consumers are known-bad.
- [x] 1.4 Update current-runtime-authority, Portfolio lifecycle, template lifecycle, and global-router FlowGuard models for author and consumer projections.
- [x] 1.5 Update the Behavior Commitment Ledger, field lifecycle, TestMesh, ModelMesh, and DevelopmentProcessFlow projections to reuse the existing model owners without duplicate boundaries.
- [x] 1.6 Run scenario, function-contract, refinement, loop/stuck, progress/fairness, BCL, ModelMesh, TestMesh, field-lifecycle, and process reviews for the changed models.

## 2. Replace Contract And Projection Schemas

- [x] 2.1 Add required `maintenance_unit_id`, `member_skill_id`, `evidence_subject_id`, and semantic ownership fields to the sole current contract source schema.
- [x] 2.2 Add disjoint `maintainer-control` and `consumer-distribution` projections to the compiler-owned content-impact model.
- [x] 2.3 Remove shared-evidence, cross-unit consumer, reuse-ticket, portable ordinary-project, and consumer control-trio authority from current schemas.
- [x] 2.4 Regenerate the compiled contract and check manifest from the new sole current source shape.
- [x] 2.5 Preserve retired schemas and payloads only as exact rejection fixtures and prove that normal runtime has no compatibility reader or fallback.

## 3. Isolate Execution Identity And Receipts

- [x] 3.1 Bind check execution keys, receipt paths, dependency validation, and success slots to maintenance unit, member, evidence subject, semantic check, owner, inputs, dependencies, toolchain, environment, and evidence domain.
- [x] 3.2 Reject foreign-unit dependency and terminal receipts before process launch or closure.
- [x] 3.3 Preserve same-unit exact-check single-flight and affected-only invalidation.
- [x] 3.4 Add semantic responsibility overlap review that returns split, merge, or retire dispositions instead of shared evidence.
- [x] 3.5 Add tests for same-unit concurrency, changed inputs, different subjects, identical commands across units, foreign receipts, and duplicate semantic owners.

## 4. Enforce Author Runtime Boundaries

- [x] 4.1 Require explicit author repository role, maintenance unit, run-state root, and owner-evidence root before supervision.
- [x] 4.2 Remove every supervisor and run-store fallback from author state to `target_root` or an ordinary business-project root.
- [x] 4.3 Add pre-write eligibility checks that reject consumer and business projects with zero filesystem or prompt changes.
- [x] 4.4 Replace portable `project-adopt` semantics with explicit maintainer-repository adoption and audit.
- [x] 4.5 Update project adoption fixtures and tests for valid author repositories, ordinary-project rejection, marker preservation, stale blocks, and zero-write failure paths.

## 5. Narrow TestMesh And Portfolio

- [x] 5.1 Restrict TestMesh plans, owners, receipt replay, and aggregation to one maintenance unit.
- [x] 5.2 Remove OpenSpec receipt projection, cross-unit consumer replay, and ordinary target dependence on SkillGuard global-prompt installation.
- [x] 5.3 Preserve frozen-plan execution, dependency order, timeout cleanup, immutable receipts, and one final full owner.
- [x] 5.4 Replace Portfolio proof networking with independent unit status aggregation and semantic-owner overlap audit.
- [x] 5.5 Retire reuse-ticket commands, schemas, fields, fixtures, and cross-unit prior-evidence acceptance from normal runtime.
- [x] 5.6 Keep unaffected unit receipts current when their exact component graph and execution identities did not change.
- [x] 5.7 Add negative tests proving Portfolio parent receipts and foreign child receipts cannot prove a unit.

## 6. Contract Global Router And Prompt Surfaces

- [x] 6.1 Change the global router from ordinary skill selection to an explicit author `managed_targets` and `external_exclusions` registry.
- [x] 6.2 Remove ordinary installed-skill contract discovery as a currentness or route prerequisite.
- [x] 6.3 Mark official OpenSpec skills external excluded and leave unregistered skills outside SkillGuard claims without blocking ordinary use.
- [x] 6.4 Replace the global prompt template with an author-maintenance-only block and exclude it from consumer installation.
- [x] 6.5 Update `skillguard-global-router/SKILL.md`, route-task entries, fixtures, CLI help, and tests for the contracted public surface.
- [x] 6.6 Remove or reject retired ordinary `resolve-global-skill` and consumer prompt installation paths without compatibility aliases.

## 7. Separate Target Templates And Prompts

- [x] 7.1 Remove `SkillGuard Runtime Rules`, contract-trio requirements, receipt consumers, and Global Router onboarding from the target consumer `SKILL.md` template.
- [x] 7.2 Move template selection, harvest, contract, and author receipt instructions into author-maintenance records.
- [x] 7.3 Stop template prompt helpers from inserting managed SkillGuard markers into consumer entrypoints.
- [x] 7.4 Update validated-template-pack adapters, fragments, profiles, schemas, fixtures, and tests for author-only supervision.
- [x] 7.5 Rewrite SkillGuard's own `SKILL.md` and validation execution policy to state the author boundary, same-unit reuse rule, consumer independence, and OpenSpec exclusion.

## 8. Split Maintainer Deployment And Consumer Distribution

- [x] 8.1 Implement an explicit maintainer deployment projection that may contain contracts, models, tests, and author-control state.
- [x] 8.2 Implement a consumer distribution builder that rejects `.skillguard/**`, SkillGuard prompts, imports, commands, receipts, Portfolio data, and router state.
- [x] 8.3 Add a preflight that blocks when target-domain runtime, data, imports, manifests, or declared references would be lost by excluding author-control paths.
- [x] 8.4 Replace the ambiguous maintained-target installer command with clearly named author deployment and consumer distribution commands.
- [x] 8.5 Preserve staged verification, atomic activation, backup, rollback, installation receipts, and ownership-aware withdrawal.
- [x] 8.6 Remove only installer-owned hash-unchanged retired files and preserve/report locally modified conflicts.
- [x] 8.7 Add clean `CODEX_HOME` tests proving consumer install and representative target work succeed without SkillGuard.

## 9. Update Documentation And Public Contracts

- [x] 9.1 Rewrite README English and Chinese sections for author cultivation, independent graduation, disjoint projections, and external provider exclusion.
- [x] 9.2 Update supervisor, execution-depth, project-adoption, target-installation, Portfolio, self-host, and suite-contract references.
- [x] 9.3 Update CLI examples, claim boundaries, version metadata, changelog, and public path/privacy checks.
- [x] 9.4 Update the author AGENTS managed block without projecting it into target repositories.
- [x] 9.5 Prove target consumer templates and public documentation contain no obsolete consumer SkillGuard dependency claims.

## 10. Validate SkillGuard Before External Migration

- [x] 10.1 Run schema, compiler, execution-record, supervisor, TestMesh, Portfolio, adoption, router, template, installation, privacy, and documentation focused tests.
- [x] 10.2 Run current model reviews and reject every new known-bad boundary.
- [x] 10.3 Freeze one self-host owner plan and complete the SkillGuard self-host run with current terminal receipts.
- [x] 10.4 Build a clean consumer fixture and prove zero SkillGuard files, prompts, processes, or project writes.
- [x] 10.5 Classify and repair every failure before external targets or installed skills are changed.

## 11. Restore External Providers And Migrate Maintained Targets

- [x] 11.1 Generate the six official OpenSpec 1.6 core skills (`propose`, `explore`, `apply`, `update`, `sync`, and `archive`) from the pinned official package, compare complete directories, and atomically replace locally injected copies including withdrawal of the retired unofficial `verify` projection.
- [x] 11.2 Verify repeated official OpenSpec update does not recreate SkillGuard sections or `.skillguard` state.
- [x] 11.3 Complete the linked FlowGuard OpenSpec change that separates its 17 consumer skills, installer, suite validation, project adoption, shadow sync, and read-only OpenSpec context.
- [x] 11.4 Migrate metadata-only maintained targets through the clean consumer projection and validate each unit independently.
- [x] 11.5 Relocate SourceGuard, WorldGuard, TraceGuard, LogicGuard, and PhysicsGuard target-domain runtime to target-owned namespaces before consumer control-state withdrawal.
- [x] 11.6 Verify native file/hash/import/data/route/check parity and representative domain work for every runtime-bearing family without SkillGuard.

## 12. Synchronize And Close

- [x] 12.1 Synchronize canonical SkillGuard and target source repositories with their consumer projections and installed Codex skill copies.
- [x] 12.2 Refresh only the author-maintenance registry and author global prompt.
- [x] 12.3 Commit the final SkillGuard and FlowGuard repository states without overwriting unrelated dirty peer work.
- [x] 12.4 Freeze source, toolchain, check inventory, impact plan, installation projections, and exactly one final full execution owner.
- [x] 12.5 Run the final full model, test, installation, consumer-isolation, official-provider, and project-zero-write validation once.
- [x] 12.6 Run a requirement-by-requirement completion audit, preserve skipped or blocked evidence, and update all OpenSpec task checkboxes only from current proof.
