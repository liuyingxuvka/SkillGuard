## 1. Current authority model

- [x] 1.1 Inventory the former runtime-authority surfaces and implement an exact residual scan that preserves current run directories and unrelated stable protocols.
- [x] 1.2 Establish the existing validation-composition FlowGuard owner and current-only TestMesh requirement without creating a parallel model.
- [x] 1.3 Replace the retirement FlowGuard model with the existing validation-composition owner's `current|blocked` direct-replacement model, including no-converter, residual, partial-replacement, and claim-boundary bad cases.
- [x] 1.4 Update source/compiled schemas and field lifecycle so old lifecycle fields are rejected by the current contract.

## 2. Current resolver and command surface

- [x] 2.1 Make the current trio plus exact residual scan the sole daily resolver input; remove retirement-receipt currentness from daily authority.
- [x] 2.2 Delete historical eligibility/completion validators, schemas, receipt chains, conversion/renewal tooling, and every maintained/installed reference to them.
- [x] 2.3 Remove former legacy compile/route/run/conversion/renewal handlers and route entries from the public CLI; keep exact rejection fixtures only.
- [x] 2.4 Update generated target guidance/templates so new maintained skills start on the current contract path and never advertise a legacy success route.
- [x] 2.5 Remove maintenance refresh and project-upgrade modes; require complete explicit current inputs for direct project adoption and reject former commands before writes.

## 3. Consumer alignment

- [x] 3.1 Move compiler checks, project adoption, provenance, clean-layout, installation, installed parity, Portfolio, and installed smoke to the `current|blocked` decision.
- [x] 3.2 Move global discovery, registry projection, route resolution, and managed prompt projection to current authority only.
- [x] 3.3 Prove no consumer escapes an isolated skill root or reads any former shape to manufacture daily success.
- [x] 3.4 Exclude receipts, reports, logs, caches, timestamps, generated status, and result sidecars from functional source identity and maintained fixture source.
- [x] 3.5 Add the existing FlowGuard compatibility-admission decision: skills use direct current replacement; ordinary-software compatibility requires an explicit historical-input requirement and bounded reader owner.
- [x] 3.6 Require exactly one native execution owner for each normalized test identity; TestMesh and receipt consumers may project or aggregate the owner receipt but may not carry or rerun its command.

## 4. Canonical affected roots

- [x] 4.1 Remove former live runtime-authority artifacts from SkillGuard and `skillguard-global-router` while preserving current run evidence and bounded audit history.
- [x] 4.2 Remove old lifecycle fields, regenerate both current contract trios, and prove exact content-impact health.
- [x] 4.3 Keep former shapes only in source-only rejection fixtures; prove they do not enter installation, Portfolio, router, or full-validation admission.

## 5. Affected regression

- [x] 5.1 Add current resolver and old-shape rejection tests, including incomplete trio, old pair only, lifecycle field, residual reappearance, current run preservation, and former-history rejection.
- [x] 5.2 Update CLI, compiler, project-adoption, provenance, installation/parity, router, prompt, and fixture regressions to current-only expectations.
- [x] 5.3 Add negative cases for former maintenance commands, runtime-output self-invalidation, broad subtree input identity, duplicate native-test ownership, consumer-carried owner commands, skill fallback/compatibility surfaces, and unapproved ordinary-software compatibility.
- [x] 5.4 Run the updated FlowGuard model and only the affected unit/schema checks; repair each exact owner before source fixpoint.

## 6. Installation and closure

- [x] 6.1 Transactionally install the affected SkillGuard suite from the frozen exact installation projection and prove canonical/stage/active current-authority parity plus rollback coverage.
- [x] 6.2 Refresh the global registry and managed prompt once from installed current roots; non-current roots remain visibly blocked without fallback.
- [x] 6.3 Run the real FlowGuard owner → SkillGuard aggregation → OpenSpec read-only receipt pilot and prove zero consumer-launched owner processes.
- [x] 6.4 Freeze source/toolchain/impact-plan identities, run exactly one final full parent under one owner, and make this change consume only its projected receipt.
- [x] 6.5 Run final JSON/privacy/diff/project audits, predictive-KB postflight, and publish a bounded report without claiming domain correctness or release from current-authority alignment alone.
