## 1. Route authority and exact admission

- [x] 1.1 Extend every current route-registry row with applicability, forbidden, required-input, mutation, first-command, next-reference, and claim-boundary fields.
- [x] 1.2 Replace keyword-score route authorization with explicit-route or typed-fact predicate evaluation and visible zero/many/forbidden blockers.
- [x] 1.3 Generate and validate the machine-readable route index and selected-route load graph from the sole current registry.

## 2. Narrow prompt projections

- [x] 2.1 Rewrite the public SkillGuard entry as a compact author gate and conditional reference loader while preserving shared hard gates.
- [x] 2.2 Rewrite the global-router skill and managed prompt template as a compact private registry pointer without route-catalog duplication.
- [x] 2.3 Add prompt-bundle, load-graph, drift, bad-route, ambiguity, forbidden, and budget/headroom fixtures.

## 3. Conditional validated-template guidance

- [x] 3.1 Upgrade target prompt generation to emit full validated-template-pack guidance in a dedicated reference only for applicable targets.
- [x] 3.2 Emit only a trigger and reference pointer in applicable target entries and assert clean consumer/install parity.
- [x] 3.3 Regenerate applicable maintained-target projections and verify target-owned selection and validation remain unchanged.

## 4. Governance and model closure

- [x] 4.1 Update FlowGuard models and checks for fact-derived routing, prompt loading, global projection, and template guidance.
- [x] 4.2 Update and compile SkillGuard's self-host contract and exact affected-check/component ownership.
- [x] 4.3 Update source, package, changelog, README, and prompt identity to patch version `0.7.2`; verify installed-author identity separately in the parent integration phase after archive.

## 5. Validation, installation, and publication

- [x] 5.1 Run affected route, prompt, template, self-host, OpenSpec, and FlowGuard checks; fix all failures.
- [x] 5.2 Hand the archived source snapshot to the parent integration phase for one final full supervised validation; do not reuse affected-only evidence as that gate.
- [x] 5.3 Hand transactional author/consumer installation, full explicit-root router refresh, and installed-currentness verification to the parent integration phase after archive.
- [x] 5.4 Complete local README/release/privacy preparation and hand commit, push, tag `v0.7.2`, GitHub Release publication, and exact remote identity verification to the parent integration phase after archive.
