## 1. Freeze Current Contract

- [x] 1.1 Record the target-specific branch-name failure and current affected modules.
- [x] 1.2 Define target-owned branch, obligation-totality, terminal-classification, and evidence-domain requirements.

## 2. Compiler Authority

- [x] 2.1 Remove target branch-name constants and historical prepared-update checks from the compiler.
- [x] 2.2 Implement generic per-branch conditional obligation totality and cross-branch applicability checks.
- [x] 2.3 Add arbitrary-branch positive fixtures and fail-closed negative compiler fixtures.

## 3. Native Terminal Runtime

- [x] 3.1 Derive branch acceptance and terminal kind from the selected target route requirement.
- [x] 3.2 Select the exact passing depth receipt without assuming scheduled production.
- [x] 3.3 Preserve scheduled installation verification only for scheduled evidence and forbid scheduled identity elsewhere.
- [x] 3.4 Update receipt schema assets and runtime documentation with no legacy branch reader.
- [x] 3.5 Repair and regression-test verified forward replacement when historical active and backup identities both drift.
- [x] 3.6 Make idempotent project adoption refresh an explicitly changed current SkillGuard version instead of silently preserving stale manifest metadata.

## 4. Verification and Installation

- [x] 4.1 Run focused compiler, conditional closure, schema, and supervisor tests.
- [x] 4.2 Run SkillGuard native self-host and declared-check validation.
- [x] 4.3 Install the whole current SkillGuard tree through the official staged installer and verify installed parity.
- [x] 4.4 Recompile the Khaos Brain update skill with only `no-update` and `explicit-manual-update`.

## 5. Closure

- [x] 5.1 Run strict OpenSpec verification and record the separate FlowGuard suite-map adoption blocker without classifying it as passed.
- [x] 5.2 Confirm no target-specific retired branch identifiers remain in SkillGuard runtime authority.

### Verification notes

- Strict OpenSpec validation passed for this change.
- SkillGuard native self-host closed the sole enforced profile with all 31 stages complete, and the official staged installer activated the same current source tree.
- Khaos Brain compiled all five maintained contracts with the installed SkillGuard; the update contract contains only `no-update` and `explicit-manual-update`.
- A scoped scan found none of the retired Khaos branch identifiers in SkillGuard runtime, schema, Skill, or supervisor authority.
- Separate blocker, not passed: the SkillGuard repository's FlowGuard project-adoption record is still at 0.56 and lacks `.skillguard/flowguard-suite/suite-map.json` for the installed 0.57 toolchain. This change does not vendor or invent the missing 17-skill FlowGuard suite.
