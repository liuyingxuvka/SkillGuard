"""Repository-local test package.

Keeping the test helpers addressable as ``tests.<module>`` prevents an
environment-installed package with the same name from shadowing this
repository's fixtures.
"""
